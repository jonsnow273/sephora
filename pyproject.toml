[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "sephora"
version = "0.1.0"
description = "The first local AI assistant with controllable internal behavior via activation steering"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Sephora Research Team", email = "team@sephora-ai.local"}
]
keywords = [
    "activation-steering",
    "mechanistic-interpretability",
    "transformer-lens",
    "local-llm",
    "ai-assistant",
    "pc-automation",
    "representation-engineering"
]

[project.scripts]
sephora = "main:main"

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]

[tool.isort]
profile = "black"
line_length = 100

[tool.ruff]
line-length = 100
select = ["E", "F", "W", "I"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
warn_unused_ignores = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--cov=. --cov-report=term-missing"
